import { ComingSoon } from '@/components/admin/ComingSoon';

export const metadata = { title: 'Inscriptions web' };

export default function SignupsPage() {
  return (
    <ComingSoon
      title="Inscriptions web"
      phase="P3"
      description="File a qualifier (awaiting / verified / rejected) du formulaire public d'inscription."
    />
  );
}
