import { ComingSoon } from '@/components/admin/ComingSoon';

export const metadata = { title: 'Ressources' };

export default function ExhibitorResourcesPage() {
  return (
    <ComingSoon
      title="Ressources exposant"
      phase="P3"
      description="CRUD exhibitor_resources : guide exposant, infos pratiques, logistique (FR + EN)."
    />
  );
}
