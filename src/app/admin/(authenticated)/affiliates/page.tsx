import { ComingSoon } from '@/components/admin/ComingSoon';

export const metadata = { title: 'Affilies' };

export default function AffiliatesPage() {
  return (
    <ComingSoon
      title="Affilies / sourcing"
      phase="P3"
      description="CRUD apporteurs d'affaires + generation lien + QR code + tracking conversions (cf. SPEC §3.13)."
    />
  );
}
